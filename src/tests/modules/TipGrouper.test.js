import { expect, test, describe, beforeEach } from "vitest";
import { mockTip, generateMockTips } from "../MockTips";

describe('TipGrouper', () => {
    describe('create empty groups', () => {
        test('', () => { });
    });

    describe('find insertion indices', () => {
        test('', () => { });

    });

    describe('group calculations', () => {
        test('', () => { });

    });

    describe('group by categories', () => {
        test('', () => { });
    });
});